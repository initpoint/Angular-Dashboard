export class Materials {
    public constructor(
        active?: boolean,
        name_ar?: string,
        name_en?: string,
        children?: any[],
        code?: string,
        parent?: any[]
    ) {}
}
