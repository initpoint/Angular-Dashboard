import {Materials} from './materials';
import {Categories} from './categories';

export class Ranking {
    public constructor(
        active?: boolean,
        name_ar?: string,
        name_en?: string,
        children?: Materials[],
        code?: string,
        parent_code?: string,
        parent?: Categories[]
    ) {

    }

}
