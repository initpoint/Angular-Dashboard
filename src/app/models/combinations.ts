export class Combinations {
    public constructor(
        isActive: boolean = true,
        isNew: boolean = true,
        nameAr?: string,
        nameEn?: string,
        hasChildren?: boolean,
        code?: string,
        type?: string,
        headId?: string,
        size?: string,
        prices?: object,
        barCodeId?: string,
        nameArFull?: string,
        materialCode?: string,
        materialNameAr?: string,
        rankingCode?: string,
        rankingNameAr?: string,
        unitCode?: string,
        unitNameAr?: string,
    ) {
    }
}
