export class Categories {
    public constructor(
        active?: boolean,
        name_ar?: string,
        name_en?: string,
        hasChildren?: boolean,
        code?: string,
        type?: string,
    ) {}
}
